{
  "name": "fitness-companion",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build -w @fc/shared && npm run build -w @fc/server && npm run build -w @fc/web",
    "dev": "concurrently -n server,web -c blue,green \"npm run dev -w @fc/server\" \"npm run dev -w @fc/web\"",
    "start": "npm run start -w @fc/server",
    "typecheck": "tsc -b packages/shared packages/server packages/web",
    "test": "npm run test -w @fc/server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  }
}
